const JoinUsButton = ({ onClick }) => (
    <button
      onClick={onClick}
      className="px-4 py-2 text-lg bg-blue-600 text-white rounded-lg hover:bg-blue-500 transition"
    >
      Join Us
    </button>
  );
  
  export default JoinUsButton;
  